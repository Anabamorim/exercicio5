#include<stdio.h>

main(){

    /* Codificar um programa que le um numero inteiro.*/

    int num;

    printf("Digite o numero:");
    scanf("%d", &num);

    if (num % 2 == 0 ){
        // se o resto da divisao der 0 o numero é par.
        printf("O numero e par");

    }
}